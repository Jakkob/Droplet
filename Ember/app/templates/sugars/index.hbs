<div class="col-sm-10 col-sm-offset-1 col-xs-10 col-xs-offset-1 well">
	{{!-- This is where I am going to insert the JS Chart --}}
	{{ember-chart type='Line' data=data height=300 legend=true}}
	{{!-- This is where I am going to insert the JS Chart --}}

	{{!-- Placeholder Table begins here: --}}
	<table class="table table-hover">
		{{index.name}}
		<tr>
			<th>Reading</th>
			<th>Date/Time</th>
		</tr>
		{{#each sugars key="id" as |sugar|}}
			<tr>
				<td>{{sugar.reading}}</td>
				<td>{{sugar.time}}</td>
			</tr>
		{{/each}}
	</table>
	{{!-- Placeholder Table end --}}

	<a class="btn btn-success center-block" href="/home/create">New Reading</a>
</div>
