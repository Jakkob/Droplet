import { sugarDial } from '../../../helpers/sugar-dial';
import { module, test } from 'qunit';

module('Unit | Helper | sugar dial');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = sugarDial(42);
  assert.ok(result);
});
